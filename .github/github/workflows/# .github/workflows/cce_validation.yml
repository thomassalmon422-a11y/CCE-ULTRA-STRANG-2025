# .github/workflows/cce_validation.yml
# CCE-ULTRA-STRANG-2025: G-SHIELD Integrity Check
# Ensures the G-SHIELD Policy Injection System (CLAIM 3) is operational.

name: CCE G-SHIELD Validation

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [ '3.10' ]

    steps:
    - uses: actions/checkout@v3
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v3
      with:
        python-version: ${{ matrix.python-version }}
    
    # This step executes the GSHIELD_Logic.py file.
    - name: Run G-SHIELD Policy Injection Test (Claim 3 Validation)
      # --- PATCH APPLIED HERE ---
      working-directory: ${{ github.workspace }} 
      run: python GSHIELD_Logic.py
      
    - name: CCE Validation Complete
      run: echo "G-SHIELD Integrity Check Passed. Policy Injection operational."
